name: ci 
on:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
      run: ls
    - name: Run a multi-line script
      run: |
        git status
        touch test.txt
        echo 'hi' > test.txt
